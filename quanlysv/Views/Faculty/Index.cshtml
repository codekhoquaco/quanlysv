@model List<QuanLySinhVien.Models.Faculty>

@{
    ViewData["Title"] = "Danh sách Khoa";
}

<h1>Danh sách Khoa 🏫</h1>

<partial name="../Shared/_search" />

<hr />

<div class="d-flex justify-content-between align-items-center mb-3">
    <a asp-action="Create" class="btn btn-primary">
        <i class="fas fa-plus"></i> Thêm Khoa Mới
    </a>
</div>

<div class="card shadow-sm">
    <div class="card-body p-0">
        <table class="table table-hover table-striped mb-0">
            <thead class="table-dark">
                <tr>
                    <th>Tên khoa</th>
                    <th>Văn phòng</th>
                    <th>Số điện thoại</th>
                    <th>Email</th>
                    <th style="width: 150px;">Hành động</th>
                </tr>
            </thead>
            <tbody>
                <partial name="_FacultyTableBody" model="Model" />
            </tbody>
        </table>
    </div>
</div>
<partial name="../Shared/_PageList" />